var x=;
console.log();

/*
For some reason on my node version (Mosh's 8 but mine's 11), a special module contents 'wrapper'
does not appear.
It basically looks like:
( function(exports, require, module, __filename, __dirname){
  // Statements from a module.
})
*/
